---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: segments.admin.solo.io
spec:
  group: admin.solo.io
  names:
    categories:
    - solo-io
    - admin
    kind: Segment
    listKind: SegmentList
    plural: segments
    shortNames:
    - seg
    singular: segment
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: DNS domain for the segment
      jsonPath: .spec.domain
      name: Domain
      type: string
    - description: Whether the segment is used by the local cluster
      jsonPath: .status.conditions[?(@.type=='Local')].status
      name: Local
      type: string
    - description: Number of connected peers
      jsonPath: .status.connectedPeers
      name: Connected
      type: integer
    - description: Number of rejected peers
      jsonPath: .status.rejectedPeers
      name: Rejected
      type: integer
    - description: Server time when this object was created.
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Segment defines a network segment with its own DNS domain
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SegmentSpec defines the desired state of Segment
            properties:
              domain:
                description: |-
                  Domain is the DNS domain for this segment.
                  Must be a valid DNS name and cannot be "cluster.local".
                maxLength: 63
                minLength: 1
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                type: string
            required:
            - domain
            type: object
          status:
            description: SegmentStatus defines the observed state of Segment
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of an object's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              connectedPeers:
                description: ConnectedPeers is the count of peers with Status=Connected
                format: int32
                type: integer
              peers:
                description: Peers lists all peer clusters that have declared this
                  segment
                items:
                  description: PeerStatus describes the observed status of a peer
                    cluster using this segment
                  properties:
                    clusterName:
                      description: ClusterName is the name of the peer cluster
                      type: string
                    hostname:
                      description: Hostname is the domain the peer is using for this
                        segment
                      type: string
                    lastTransitionTime:
                      description: LastTransitionTime is when the status last changed
                      format: date-time
                      type: string
                    reason:
                      description: Reason explains why the peer is in the current
                        status (e.g., HostnameMismatch)
                      type: string
                    status:
                      description: Status indicates the peer's connection status (Connected,
                        Rejected, etc)
                      type: string
                  type: object
                type: array
              rejectedPeers:
                description: RejectedPeers is the count of peers with Status=Rejected
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
