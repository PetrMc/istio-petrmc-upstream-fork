# Unique App Certs

This directory contains certificates unique to some of our echo apps. This can be used to test multiple apps egressing with their own unique identity

The certificates are signed with pilot/ca-key.pem
